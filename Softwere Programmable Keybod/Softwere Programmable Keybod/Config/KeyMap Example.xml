<?xml version="1.0" encoding="UTF-8"?>
<!--
下記フォルダにKey Map名となるフォルダを作成し当ファイルを適宜編集しKeyMap.xmlのファイル名で保存すると、Key Mapとして認識されます。
%USERPROFILE%\AppData\Local\Theia.ws\Softwere Programmable Keybod\1.0.0.0

またKeyMap.xml及びそのKey Mapで使用している画像をZIPファイルとして圧縮するとインポート可能なKey Mapとなります。
KeyMap.xmlはZIPの直下に配置してください。
-->
<keyMapSet windowHeight="xxx" windowWidth="xxx">
<!--
windowHeight、windowWidth:
ウィンドウ入力部分のサイズをピクセル単位で指定。
キーのキーの間に10pxのマージン、上下左右に10pxのマージンが確保され、
格子状に分割された空間にキーが配置される。
指定しない場合はキー1つを100px四方としてウィンドウサイズを自動定義。
-->
<extends>xxx</extends>
<extends>xxx</extends>
<!--
extends:
継承元となるキーマップフォルダ名を指定。
-->
<targetExe>*.exe</targetExe>
<targetExe>*.exe</targetExe>
<!--
このキーマップセットを適用する対象のアプリケーション名。（未使用）
-->
<keyMap default="xxxxx" height="xxxxx" name="xxxxx" width="xxxxx">
<!--
default:
キーマップを初期状態に指定するフラグ。（1=初期状態、0=初期状態ではない）
初期状態のキーマップは必ず1つにしなければならない。

height:
縦方向のキー数。
未指定の場合、最も縦方向が長いキーマップに合わせて調整される。
指定した場合、キーサイズが自動調整され画面全体を覆うようになる。

name:
キーマップ名。
キーマップセットで一意な名前でなければならない。

width:
横方向のキー数。
未指定の場合、最も縦方向が長いキーマップに合わせて調整される。
指定した場合、キーサイズが自動調整され画面全体を覆うようになる。
-->
<extends>xxx</extends>
<extends>xxx</extends>  
<!--
extends:
継承元となるキーマップ名を指定。
-->
<row>
<!--
keyを指定しない場合、空行として表示される。
-->
<key afterMap="xxxxx" height="xxxxx" keyTop="xxxxx" keyTopActiveImage="xxxxx" keyTopHoverImage="xxxxx" keyTopImage="xxxxx" width="xxxxx">
<!--
afterMap:
キー操作完了後に表示するキーマップ名。

height:
キーの高さ。
通常のキーサイズは1、高さ2倍なら2を指定する。

keyTop:
キートップに表示する文字列。

keyTopActiveImage:
キーアクティブ化時にキートップに表示する画像のパス。

keyTopHoverImage:
ポインターホバー時にキートップに表示する画像のパス。

keyTopImage:
キートップに表示する画像のパス。

width:
キーの幅。
通常のキーサイズは1、幅2倍なら2を指定する。

inputDataをしていない場合:
空ボタンとして描画上は空き領域となる。
-->
<inputData afterWaite="xxxxx" index="xxxxx">xxxxx</inputData>
<!--
afterWaite:
インプットデータを流し込んだ後待機する秒数。
m秒で指定する。
キーマップ間の遷移や、次の入力を阻害することとなる為、あまり大きな値は設定しないこと。

index:
インプットデータの流し込み順序。key内で一意な数値でなければならない。

要素値:
入力するデータ。記述方法は下記URL参照。
https://msdn.microsoft.com/en-us/library/system.windows.forms.sendkeys.send(v=vs.110).aspx

入力、待機、入力のように時間を空けて入植したい場合、
inputDataを複数指定すればよい。
-->
</key>
</row>
</keyMap>
</keyMapSet>